/**\n * Authentication API Usage Examples\n * \n * This file demonstrates how to use the comprehensive authentication API.\n */\n\nimport {\n  AuthAPI,\n  TokenManager,\n  VerificationAPI,\n  AuthMiddleware,\n  AuthGuard,\n  type LoginRequest,\n  type RegisterRequest\n} from './authentication';\n\n/**\n * Example: User Login\n */\nexport async function exampleLogin() {\n  const loginData: LoginRequest = {\n    email: 'user@example.com',\n    password: 'password123',\n    rememberMe: true\n  };\n\n  try {\n    const result = await AuthAPI.login(loginData);\n    \n    if (result.success) {\n      console.log('Login successful:', result.user);\n      console.log('Access token:', result.token);\n      \n      // Tokens are automatically stored in localStorage\n      // You can also access them later:\n      const storedToken = TokenManager.getAccessToken();\n      const storedUser = TokenManager.getStoredUser();\n      \n      return { success: true, user: result.user };\n    } else {\n      console.error('Login failed:', result.message);\n      return { success: false, error: result.message };\n    }\n  } catch (error) {\n    console.error('Login error:', error);\n    return { success: false, error: 'Network error' };\n  }\n}\n\n/**\n * Example: User Registration\n */\nexport async function exampleRegistration() {\n  const registrationData: RegisterRequest = {\n    fullName: 'John Doe',\n    username: 'johndoe123',\n    email: 'john@example.com',\n    phoneNumber: '+1234567890',\n    password: 'SecurePass123!',\n    city: 'New York',\n    state: 'NY',\n    country: 'USA',\n    zipCode: '10001',\n    agreeToTerms: true,\n    marketingOptIn: false,\n    allowLocationPermission: true,\n    shoeSize: '10',\n    favoriteBrands: ['Nike', 'Adidas'],\n    buyingPreference: 'both'\n  };\n\n  try {\n    const result = await AuthAPI.register(registrationData);\n    \n    if (result.success) {\n      console.log('Registration successful:', result.user);\n      console.log('Verification required:', result.requiresVerification);\n      \n      return { \n        success: true, \n        user: result.user,\n        needsVerification: result.requiresVerification \n      };\n    } else {\n      console.error('Registration failed:', result.error || result.message);\n      return { success: false, error: result.error || result.message };\n    }\n  } catch (error) {\n    console.error('Registration error:', error);\n    return { success: false, error: 'Network error' };\n  }\n}\n\n/**\n * Example: Email Verification\n */\nexport async function exampleEmailVerification(verificationToken: string) {\n  try {\n    const result = await VerificationAPI.verifyEmail(verificationToken);\n    \n    if (result.success) {\n      console.log('Email verified successfully');\n      return { success: true };\n    } else {\n      console.error('Email verification failed:', result.error);\n      return { success: false, error: result.error };\n    }\n  } catch (error) {\n    console.error('Email verification error:', error);\n    return { success: false, error: 'Network error' };\n  }\n}\n\n/**\n * Example: Phone Verification\n */\nexport async function examplePhoneVerification(userId: string, verificationCode: string) {\n  try {\n    const result = await VerificationAPI.verifyPhone(userId, verificationCode);\n    \n    if (result.success) {\n      console.log('Phone verified successfully');\n      return { success: true };\n    } else {\n      console.error('Phone verification failed:', result.error);\n      return { success: false, error: result.error };\n    }\n  } catch (error) {\n    console.error('Phone verification error:', error);\n    return { success: false, error: 'Network error' };\n  }\n}\n\n/**\n * Example: Making Authenticated Requests\n */\nexport async function exampleAuthenticatedRequest() {\n  try {\n    // Check if user is authenticated first\n    if (!AuthGuard.isAuthenticated()) {\n      console.log('User not authenticated');\n      return { success: false, error: 'Not authenticated' };\n    }\n\n    // Get user profile using authenticated request\n    const profile = await AuthAPI.getProfile();\n    \n    if (profile.success) {\n      console.log('User profile:', profile.user);\n      console.log('Can sell:', profile.canSell);\n      console.log('Verification status:', profile.verificationStatus);\n      \n      return { success: true, profile: profile.user };\n    } else {\n      console.error('Failed to get profile:', profile.message);\n      return { success: false, error: profile.message };\n    }\n  } catch (error) {\n    console.error('Profile fetch error:', error);\n    return { success: false, error: 'Network error' };\n  }\n}\n\n/**\n * Example: Password Change\n */\nexport async function examplePasswordChange(\n  currentPassword: string,\n  newPassword: string,\n  confirmPassword: string\n) {\n  try {\n    const result = await AuthAPI.changePassword({\n      currentPassword,\n      newPassword,\n      confirmPassword\n    });\n    \n    if (result.success) {\n      console.log('Password changed successfully');\n      return { success: true };\n    } else {\n      console.error('Password change failed:', result.message);\n      return { success: false, error: result.message };\n    }\n  } catch (error) {\n    console.error('Password change error:', error);\n    return { success: false, error: 'Network error' };\n  }\n}\n\n/**\n * Example: Logout\n */\nexport async function exampleLogout() {\n  try {\n    const result = await AuthAPI.logout();\n    console.log('Logout result:', result.message);\n    \n    // Verify that tokens are cleared\n    const isStillLoggedIn = TokenManager.isLoggedIn();\n    console.log('Still logged in:', isStillLoggedIn); // Should be false\n    \n    return { success: true };\n  } catch (error) {\n    console.error('Logout error:', error);\n    return { success: false, error: 'Logout failed' };\n  }\n}\n\n/**\n * Example: Authentication State Management\n */\nexport function exampleAuthStateManagement() {\n  // Setup automatic token refresh\n  AuthMiddleware.setupAutoRefresh();\n  \n  // Get current authentication state\n  const authState = AuthMiddleware.getAuthState();\n  console.log('Auth state:', authState);\n  \n  // Check specific user permissions\n  if (AuthGuard.isAuthenticated()) {\n    const user = AuthGuard.getUser();\n    console.log('Current user:', user);\n    \n    // Check if user has admin role\n    if (AuthGuard.hasRole('ADMIN')) {\n      console.log('User is admin');\n    }\n    \n    // Check if user has any of multiple roles\n    if (AuthGuard.hasAnyRole(['ADMIN', 'MODERATOR'])) {\n      console.log('User has elevated permissions');\n    }\n  }\n  \n  return authState;\n}\n\n/**\n * Example: Token Validation and Refresh\n */\nexport async function exampleTokenManagement() {\n  try {\n    // Validate current token\n    const validation = await TokenManager.validateToken();\n    \n    if (validation.valid) {\n      console.log('Token is valid:', validation.user);\n      return { success: true, user: validation.user };\n    } else {\n      console.log('Token validation failed:', validation.message);\n      \n      // Try to refresh token\n      const refreshResult = await TokenManager.refreshAccessToken();\n      \n      if (refreshResult.success) {\n        console.log('Token refreshed successfully');\n        return { success: true, tokenRefreshed: true };\n      } else {\n        console.log('Token refresh failed:', refreshResult.message);\n        // User needs to login again\n        return { success: false, needsLogin: true };\n      }\n    }\n  } catch (error) {\n    console.error('Token management error:', error);\n    return { success: false, error: 'Token management failed' };\n  }\n}\n\n/**\n * Example: Complete Authentication Flow\n */\nexport async function exampleCompleteAuthFlow() {\n  console.log('=== Complete Authentication Flow Example ===');\n  \n  // 1. Check if already logged in\n  if (TokenManager.isLoggedIn()) {\n    console.log('User already logged in');\n    const user = TokenManager.getStoredUser();\n    console.log('Stored user:', user);\n    return { success: true, alreadyLoggedIn: true, user };\n  }\n  \n  // 2. Register new user\n  console.log('Registering new user...');\n  const registrationResult = await exampleRegistration();\n  \n  if (!registrationResult.success) {\n    console.error('Registration failed');\n    return registrationResult;\n  }\n  \n  // 3. Handle verification if needed\n  if (registrationResult.needsVerification?.email) {\n    console.log('Email verification required');\n    // In real app, user would receive email and click verification link\n    // For demo, we'll skip this step\n  }\n  \n  if (registrationResult.needsVerification?.phone) {\n    console.log('Phone verification required');\n    // In real app, user would receive SMS and enter verification code\n    // For demo, we'll skip this step\n  }\n  \n  // 4. Get user profile to verify everything is working\n  const profileResult = await exampleAuthenticatedRequest();\n  \n  if (!profileResult.success) {\n    console.error('Failed to get profile after registration');\n    return profileResult;\n  }\n  \n  // 5. Setup auto-refresh for the session\n  AuthMiddleware.setupAutoRefresh();\n  \n  console.log('=== Authentication Flow Complete ===');\n  return { \n    success: true, \n    user: registrationResult.user,\n    profile: profileResult.profile\n  };\n}\n\n// Export all examples as a collection\nexport const AuthExamples = {\n  login: exampleLogin,\n  registration: exampleRegistration,\n  emailVerification: exampleEmailVerification,\n  phoneVerification: examplePhoneVerification,\n  authenticatedRequest: exampleAuthenticatedRequest,\n  passwordChange: examplePasswordChange,\n  logout: exampleLogout,\n  authStateManagement: exampleAuthStateManagement,\n  tokenManagement: exampleTokenManagement,\n  completeFlow: exampleCompleteAuthFlow\n};